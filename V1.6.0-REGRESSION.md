# Starlight v1.6.0 多角色自定义栏位回归说明

## 数据结构

- 每个剧情智能体至少包含一名角色。
- 每名角色至少包含一个自定义栏位。
- 新剧情的角色1默认只有“角色名称、角色外貌、角色性格”三栏。
- 新角色只复制上一名角色的栏名与顺序，栏位内容和内部 ID 均重新创建。
- 旧版单角色字段在读取时迁移到角色1，不删除旧聊天、收藏或长期记忆。

## 建议回归

1. 新建剧情，确认默认只显示角色1及三个初始栏位。
2. 将角色1扩展为五栏并修改栏名，添加角色2，确认五个栏名和顺序一致但内容为空。
3. 分别编辑角色1和角色2，关闭后重新打开，确认内容独立保存且不会互相同步。
4. 删除一个普通栏位，确认最后一栏无法删除。
5. 添加三名角色并删除中间角色，确认显示编号连续重排，最后一名角色无法删除。
6. 继续剧情对话，检查请求提示词中各角色以“角色1、角色2……”分隔。
7. 升级旧安装，确认原基础人设、外貌、状态、成长、关系、知识边界、目标和对白示例出现在角色1中。
8. 在房间创建页从多角色剧情中选择一个具体角色复制，确认未混入同剧情的其他角色。

## 自动检查

```powershell
npm test
npx cap sync android
```

## 版本

- Android `versionCode 10`
- Android `versionName 1.6.0`
- npm package `1.6.0`
