# Starlight v1.3.1 房间模式修复说明

## 修复内容

### 1. 背景画幅

旧版使用 `background-attachment: local`，浏览器会按照聊天框的全部滚动内容高度计算背景。长对话重新打开时，滚动内容很高，因此背景可能出现异常拉伸或压缩。

新版改为按照当前可见聊天框计算背景画幅，并分别固定遮罩层和图片的尺寸。原来上传的背景可以继续使用，不需要重新上传。

### 2. 重复角色名和空行

旧版会把每一条历史AI消息转换为以下格式后再次发给模型：

```text
【角色名】
回复正文
```

历史越长，模型看到的相同模板越多，容易逐渐把 `【角色名】` 学成必须重复的输出格式。

新版调整为：

- 双人房间中，当前角色的历史回复以纯正文进入上下文，不再添加姓名标题。
- 多人房间中，其他角色的发言使用自然语言说明来源，不把姓名标签伪装成AI输出模板。
- 自动移除旧记录和新回复开头重复的 `【角色名】`、`角色名：`、只有姓名的空行等内容。
- 连续三个以上空行会压缩为正常段落间距。
- 如果模型第一次只返回姓名标签或空行，自动追加纠正要求并重试一次。
- 第二次仍然无有效内容时直接跳过，不保存空消息气泡。

### 3. 回复节奏

- 很短的用户消息：提示模型优先使用一到三句或一个短段落。
- 一般对话：通常一小段，必要时才增加第二段。
- 用户明确要求详细描写、解释或长篇内容：允许按内容自然展开。
- 明确禁止固定三段以及机械的“动作—台词—心理”结构。

这些是节奏引导，不会把所有回答强行限制成同一长度。

## 版本

- `versionCode 5`
- `versionName 1.3.1`
- `package.json 1.3.1`

## 替换与提交

将修复包里的文件复制到 `D:\project_files\starlight` 的相同位置：

- `www/index.html`
- `www/rooms.js`
- `package.json`
- `android/app/build.gradle`
- `UPLOAD.md`

`memory.js` 没有修改，包中保留了一份完整副本。永久签名工作流和 GitHub Secrets 没有改动。

```powershell
Set-Location 'D:\project_files\starlight'

git add www/index.html www/rooms.js package.json android/app/build.gradle UPLOAD.md
git status
git commit -m "fix: 修复房间背景和重复角色名输出"
git push origin main
```

## 建议测试

1. 打开原来已经上传背景的房间，切换到其他房间后返回，再彻底关闭应用并重新打开。
2. 在存在较长历史的双人房间继续短对话，确认旧回复中的连续姓名标签被清理。
3. 连续发送“坐好了吗”“嗯？”“然后呢”等短句，观察回复是否能自然缩短到一句或一小段。
4. 发送明确的“详细描写接下来发生的事情”，确认仍可以生成较长内容。
5. 在多人房间指定不同角色发言，确认角色来源仍能正确识别。
6. 检查通用模式、亲密度、关系节点、撤回和重新生成是否正常。

首次用新版打开时，会对房间内AI消息进行保守清理：只删除与该消息角色姓名完全一致的标签和多余空行，不删除正文内容。
