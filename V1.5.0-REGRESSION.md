# Starlight v1.5.0 剧情／房间隔离升级说明

## 页面边界

- 剧情模式：单角色持续剧情、结构化角色卡、剧情定位、收藏、可编辑长期记忆。
- 房间模式：双人或多人角色调度、亲密度、房间公共记忆与角色私人记忆。
- 两个页面只共享 DeepSeek API Key 与本机数据库；模型选择、提示词、记忆配置、对话、渲染窗口与当前目标均分离。
- “从剧情模式复制角色”是用户主动触发的一次性复制，之后不会同步。

## 升级兼容

- 旧 `activeMode: general` 自动迁移为 `story`。
- 旧 `surfacePrompt` 自动迁移为剧情角色的 `characterPrompt`。
- 旧的单份 `memorySettings` 首次升级时分别复制给剧情和房间，之后独立保存。
- 既有消息、收藏、长期记忆、房间、亲密度和图片原样保留。

## 建议回归

1. 升级旧安装后分别进入剧情与房间，确认原智能体、房间和聊天记录存在。
2. 在剧情页修改输出上限或关闭长期记忆，切换房间，确认房间设置不跟随变化。
3. 创建剧情智能体，确认立即打开全屏设定页并能保存全部分项字段。
4. 打开剧情“记忆”页，修改核心记忆、篇章标题、关键词、重要度和摘要后重新打开核对。
5. 在房间页确认不显示剧情定位、收藏和剧情记忆入口。
6. 从剧情角色一次性复制到房间，随后修改剧情角色，确认房间角色不会同步变化。
7. 分别进行长剧情与多人房间对话，确认两边记忆和提示词没有串线。

## 自动检查

```powershell
npm test
npx cap sync android
```

## 版本

- Android `versionCode 9`
- Android `versionName 1.5.0`
- npm package `1.5.0`
