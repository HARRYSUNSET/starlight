# Starlight v1.3.3 模型升级说明

## 本版变更

- 根据 DeepSeek 官方“模型 & 价格”文档，将 Flash 模型标识从已下线的 `deepseek-v4-flash` 更新为官方当前推荐的 `deepseek-flash`。
- `deepseek-flash` 当前对应 DeepSeek-V4.1-Flash；`deepseek-v4-pro` 当前对应 DeepSeek-V4-Pro-0813。
- 完整移除 OpenRouter 和 Liquid LFM2.5-2.6B 免费模型的界面、请求头、模型选择和本地配置。
- 旧 DeepSeek 模型配置会自动迁移，聊天、智能体、房间、长期记忆和本地图片不受影响。

官方依据：<https://api-docs.deepseek.com/zh-cn/quick_start/pricing/>

## 建议回归

1. 从 v1.3.2 覆盖安装，确认原有 DeepSeek API Key、智能体、房间和聊天记录仍存在。
2. 打开模型列表，确认只显示 DeepSeek V4.1 Flash 和 DeepSeek V4 Pro 0813。
3. 分别使用两个模型进行普通聊天和房间聊天，确认流式输出正常。
4. 开启与关闭深度思考，确认请求均可正常完成。
5. 触发长期记忆、多人智能调度和双人亲密度判定，确认 JSON 请求正常。

## 自动检查

```powershell
npm test
npx cap sync android
Set-Location android
.\gradlew.bat assembleDebug
```

## 版本

- Android `versionCode 7`
- Android `versionName 1.3.3`
- npm package `1.3.3`
