# Starlight v1.4.0 长对话与剧情系统升级说明

## 设计目标

大模型本身始终存在有限上下文窗口，因此本版不把全部历史无限塞入单次请求，而是采用分层记忆：完整原文永久留在本机，近期原文作为工作记忆，旧内容整理成持续状态和篇章记忆，再按当前话题检索相关往事。这样可以让会话持续增长到本机存储空间耗尽，同时把单次 API 请求保持在稳定范围内。

## 主要变更

- 近期上下文、长期记忆与模型输出分别保留 token 预算，避免几百轮后请求持续膨胀直至失败。
- 普通模式与房间模式都会分批整理旧对话；整理失败时继续使用安全的近期窗口生成，不再卡死当前会话。
- 房间长期记忆分为公共核心、公共篇章、角色私人核心与角色私人篇章。
- 新增对话全文搜索、消息编号跳转、收藏和前后分窗加载。
- 房间编辑器升级为全屏页面，角色成长、当前状态和关系变化可以独立维护。
- 新增故事文风示例以及面向轻松日常剧情的默认文风提示词。

## 建议回归

1. 在包含数百条消息的智能体和房间中继续发送，确认记忆整理失败不会阻断正常回复。
2. 使用定位器搜索旧台词、输入消息编号、收藏消息并在前后窗口间切换。
3. 覆盖安装旧版本，确认聊天、房间、图片、亲密度和原有记忆不丢失。
4. 打开房间设置，确认全屏编辑页可保存新增的角色成长、当前状态、关系、知识边界、对白示例和故事文风示例。
5. 进行多人房间长对话，确认不同角色仍只读取自己的私人记忆。
6. 用短句进行轻松日常对话，再明确要求详细剧情，确认回复长度和文风能随场景变化。

## 自动检查

```powershell
npm test
npx cap sync android
Set-Location android
.\gradlew.bat assembleDebug
```

## 设计参考

- DeepSeek 多轮对话：API 本身无状态，客户端需要在每次请求中提供所需历史。<https://api-docs.deepseek.com/zh-cn/guides/multi_round_chat>
- DeepSeek 上下文缓存：相同前缀会自动命中缓存，适合固定人设与持续对话。<https://api-docs.deepseek.com/zh-cn/guides/kv_cache>
- MemGPT：使用分层记忆与虚拟上下文管理突破固定上下文窗口。<https://arxiv.org/abs/2310.08560>
- Think-in-Memory：从长历史中检索与当前问题相关的记忆。<https://arxiv.org/abs/2311.08719>

## 版本

- Android `versionCode 8`
- Android `versionName 1.4.0`
- npm package `1.4.0`
