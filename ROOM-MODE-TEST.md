# Starlight v1.3.0 房间模式测试说明

## 本版包含

- 通用模式与房间模式独立切换，现有智能体、聊天原文和长期记忆保持原样。
- 房间模式分为双人房间和多人房间；房间创建后类型固定，避免破坏既有记录。
- 双人房间拥有 0—200 亲密度、隐藏人物判定、代码限速和自定义关系节点。
- 多人房间支持智能调度、指定角色和全员一轮。
- 每个房间都有独立的世界观、场景、用户角色设定、文风、公共记忆和角色独立记忆。
- 角色头像和聊天背景保存到本机 IndexedDB，保存前会自动压缩；图片不会发送给 DeepSeek。
- 支持整轮撤回、单角色重新生成、停止发言队列、让场景继续。

## 替换文件

先备份当前项目。将测试包里的文件复制到 `D:\project_files\starlight` 的相同位置：

- `www/index.html`
- `www/rooms.js`（新增文件，不能漏掉）
- `www/memory.js`
- `package.json`
- `android/app/build.gradle`
- `UPLOAD.md`

本包没有修改 `.github/workflows/main.yml`，会继续使用已经配置好的永久签名和 GitHub Secrets。

提交前确认：

```powershell
Set-Location 'D:\project_files\starlight'

git add www/index.html www/rooms.js www/memory.js package.json android/app/build.gradle UPLOAD.md
git status
git commit -m "feat: 新增双人和多人房间模式"
git push origin main
```

版本号已更新为：

- `versionCode 4`
- `versionName 1.3.0`
- npm package `1.3.0`

## 建议测试顺序

### 1. 通用模式回归

打开通用模式，确认原有智能体、聊天记录、提示词、重新生成、撤回、重启对话和长期记忆仍能使用。

### 2. 双人房间

1. 创建双人房间。
2. 填写世界观、场景和“用户所扮演的角色设定”。
3. 创建角色，或从通用模式复制一个智能体。
4. 上传角色头像和聊天背景。
5. 添加几个亲密度节点，例如 `20`、`50`、`100`。
6. 保存后等待提示“判定已启用，可进行攻略”。
7. 进行普通寒暄、有效关心、冒犯边界等不同类型的对话，观察亲密度是否以较慢速度变化。
8. 测试整轮撤回是否同步撤销本轮亲密度变化。

亲密度代码上限：普通单轮最多 `+2`，60 以上单轮最多 `+1`；连续多轮上涨会继续减速；170 以上只有被判定为重要事件时才可上涨。普通交流应经常为 `0`。

### 3. 多人房间

1. 创建至少三名角色。
2. 使用智能调度，确认每轮通常只有一至两人回应。
3. 使用输入框上方的角色菜单指定一人回应。
4. 在文本中输入 `@角色名` 测试点名。
5. 切换为“全员一轮”，确认角色按顺序生成，并且后发言者能看到前一人的回答。
6. 生成中点击停止，确认剩余队列不会继续。

### 4. 模式隔离

在两个模式中分别发送消息、修改房间文风并重启房间，确认通用模式智能体的提示词和记录没有改变。

## API 请求数量

- 双人房间正常一轮：1 次角色回复请求 + 1 次亲密度 JSON 判定。
- 多人房间智能调度：1 次小型 JSON 调度 + 实际发言人数对应的回复请求。
- 指定单个角色：不调用发言调度器。
- 长期记忆达到阈值后，会额外产生记忆整理请求。

若发现问题，请保留发生问题的房间、操作顺序、界面截图和 GitHub Actions 构建日志，不要先清空数据。
