# 更新记录

## v1.6.0

- 剧情模式角色设定升级为多角色结构，一个剧情中可保存任意数量的独立角色卡
- 新剧情默认保留“角色1”，初始提供“角色名称、角色外貌、角色性格”三栏
- 每个角色的栏位名称和内容均可编辑，并可自由添加或删除栏位；每名角色始终至少保留一栏
- 角色区新增“添加角色”和“删除角色”操作，整个剧情始终至少保留一名角色，显示编号会随顺序自动整理
- 新增角色会复制上一名角色当时的栏位名称与排列顺序，但所有内容保持空白，便于连续建立结构一致的角色
- 剧情提示词会使用“角色1、角色2……”明确分隔人物资料，并加强姓名、经历、知识、秘密、目标和说话方式的隔离
- 旧版单角色智能体自动迁移为“角色1”的自定义栏位，原有外貌、状态、成长、关系、知识边界、目标、说话方式与对白示例不会丢失
- 从剧情模式向房间复制人物时，现在可以准确选择某个剧情中的具体角色，仍然只进行一次性复制
- Android 版本更新为 `versionCode 10`、`versionName 1.6.0`

## v1.5.0

- 将原“通用模式”正式重构为独立的“剧情模式”，顶部使用剧情/房间双页面切换，不再把剧情创作功能误归入房间模式
- 剧情模式与房间模式拆分模型选择、长期记忆设置、思考模式、输出上限、消息渲染窗口与页面状态；剧情底层提示词绝不会进入房间请求
- 侧边栏改为按需打开的作品抽屉，主聊天区获得完整宽度；抽屉会根据当前页面只展示剧情书架或房间列表
- 新增剧情智能体全屏结构化设定页，创建角色时直接进入完整页面，可分项填写世界、场景、用户角色、基础人设、成长、状态、关系、知识边界、目标、对白和文风
- 新增独立 `story.js` 剧情提示词模块；当前状态与已经发生的成长高于早期静态标签，旧版表层提示词会自动迁移为基础人设
- 新增剧情长期记忆管理页，可查看和修改核心记忆、篇章标题、摘要、关键词与重要度，也可保留完整原文并清空整理结果后重建
- 对话定位、消息收藏和可编辑长期记忆明确归属剧情模式；房间页面不再显示这些剧情专用入口
- 从剧情角色复制到房间改为明确的一次性复制，之后双方设定、消息和记忆互不联动
- Android 版本更新为 `versionCode 9`、`versionName 1.5.0`

## v1.4.0

- 重构超长对话上下文：完整原文继续保存在 IndexedDB，只向模型发送受 token 预算控制的近期原文、持续状态与相关往事，单个会话可持续累积到本机存储空间耗尽
- 长期记忆改为分层结构，新增篇章标题、重要度、结构化核心状态、话题检索预算和角色私人篇章记忆
- 记忆整理改为分批且失败开放：单次整理异常不再阻断正常回复，未整理原文仍会保留并在后续轮次继续处理
- 新增“对话定位”页面，支持全文搜索、按消息编号跳转、关键消息收藏、最近消息列表和分窗加载，长对话无需反复向上翻页
- 房间设置改为全屏剧情编辑页，新增故事文风示例和其他剧情信息输入
- 角色设定拆分为基础档案、公开身份、当前状态、成长记录、关系网络、知识边界、说话行动风格、私人目标和对白示例
- 强化剧情提示词的状态优先级：当前状态与已经发生的成长高于早期静态性格标签，减少角色为了贴合人设而机械说话
- 新增轻松日常剧情文风，引导自然接话、生活化动作、反应节奏、关系反差和轻小说式对白，同时避免固定三段与模板化动漫腔
- Android 版本更新为 `versionCode 8`、`versionName 1.4.0`

## v1.3.3

- 按 DeepSeek 官方模型文档将默认 Flash 模型更新为 `deepseek-flash`，对应 DeepSeek-V4.1-Flash
- 保留 `deepseek-v4-pro`，并在界面明确其当前版本为 DeepSeek-V4-Pro-0813
- 移除 OpenRouter 与 Liquid LFM2.5-2.6B 免费模型，不再保存或使用 OpenRouter 配置
- 旧版 `deepseek-v4-flash`、`deepseek-v4-flash-vision-exp`、`deepseek-chat` 和 `deepseek-reasoner` 配置会自动迁移到 `deepseek-flash`
- 保留旧 `deepseek-reasoner` 用户的深度思考设置，升级不会影响现有智能体、房间、聊天记录与长期记忆
- Android 版本更新为 `versionCode 7`、`versionName 1.3.3`

## v1.3.2

- 强化通用模式、智能体、房间和生成任务之间的状态隔离，生成中不再允许切换、删除或修改当前对话目标
- 修复房间长期记忆过滤空回复后消息序号错位的问题，公共记忆与角色独立记忆继续按原始房间消息边界整理
- 修复选择背景或头像后立即保存时，异步图片压缩尚未完成而导致图片漏存或被旧图覆盖的问题
- 房间保存会等待图片处理并确认 IndexedDB 写入完成，保存失败时不再误报成功
- 流式生成期间不再自动滚动，完成、停止或失败后保持用户当前阅读位置；移动端滚动不会继续传递给整个页面
- 扩展角色姓名标签清理规则，并将空标签自动重试由一次增加为两次
- 修复通用模式重新生成失败或被停止时旧回复丢失的问题
- 修复编辑双人房间其他设置时，未修改亲密度却悄悄改变重启初始值的问题
- 修复智能体列表反复渲染时重复注册全局点击监听器的问题
- 云端 Android 构建改为严格使用 package-lock，避免依赖漂移造成偶发打包失败
- 补充房间输出、记忆边界、版本一致性和关键页面行为的自动化回归测试

## v1.3.1

- 修复房间背景在长对话重新打开后按全部滚动内容计算画幅而发生异常拉伸的问题
- 移除发送给模型的重复角色姓名模板，避免长对话逐渐学会反复输出姓名标签
- 自动清理旧记录和新回复中的重复姓名行、多余空行与空白标签
- 无效空回复会自动重试一次；重试仍为空时跳过，不再保存空消息气泡
- 根据用户本轮消息长度动态提示回复节奏，短对话优先一到三句或一个短段落
- 明确禁止每轮固定三段以及机械的“动作—台词—心理”结构
- 保留房间记录、亲密度、角色设定、上传图片以及通用模式数据

## v1.3.0 测试版

- 新增完全独立的“房间模式”，可在通用模式和房间模式之间切换
- 房间模式支持双人房间与多人房间
- 双人房间新增0—200亲密度、隐藏人物判定和用户自定义关系节点
- 亲密度在提示词与代码两层限速，普通交流不会快速累积
- 多人房间新增智能发言调度、指定角色和全员一轮
- 新增用户所扮演角色的独立设定
- 新增房间公共记忆与角色独立记忆
- 支持本地上传角色头像和聊天背景，并在保存前自动压缩
- 支持整轮撤回、单角色重新生成、停止队列和让场景继续
- 保留通用模式全部现有功能与历史数据

## v1.2.0

- 新增IndexedDB大容量聊天存储
- 新增自动长期记忆和分段往事检索
- 强化底层提示词和智能体人设
- 新增可编辑的自然文风提示词
- 新增存储配额和持久保护状态
- 更新DeepSeek V4 Flash和V4 Pro模型
- 优化超长聊天的页面渲染性能
- 保留创建、编辑、撤回、重新生成和重启对话等原有功能
