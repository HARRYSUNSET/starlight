# Starlight v1.3.2 回归说明

## 本版重点

本版只修复可靠性与隔离问题，不移除或改名任何既有功能。通用模式、双人房间、多人房间、亲密度、长期记忆、头像、背景、旁白、重新生成、撤回、继续场景和两种 API 服务商均保留。

## 建议手工回归

1. 在通用模式开始生成，确认生成期间无法切换、删除或编辑智能体；停止后可正常操作。
2. 在房间模式开始多人队列，确认生成期间无法切换、删除或编辑房间，且各条回复只写入发起生成的房间。
3. 生成长回复时把聊天区停在任意位置，确认文字增长、生成完成、停止和报错都不会自动滚动；只有手指或鼠标滚轮会改变阅读位置。
4. 选择房间背景或角色头像后立刻点击保存，彻底关闭并重开应用，确认图片仍存在。
5. 连续快速选择两张背景，确认最后一次选择生效；选择图片后点击移除，确认较早的压缩任务不会把图片重新放回。
6. 在双人房间把亲密度推进后，只编辑场景或文风并保存，再重启对话，确认重启初始值没有被当前进度暗中覆盖。
7. 让模型输出 `角色名：`、`### 角色名`、`【角色名】` 或重复姓名行，确认这些标签不保存为空气泡；无有效正文时最多自动重试两次。
8. 在通用模式点击重新生成后立刻停止，或模拟 API 报错，确认原回复自动恢复。
9. 对已有长房间继续聊天并触发长期记忆，确认摘要消息号与原房间消息号一致，不会提前丢弃近期内容。

## 自动检查

在项目根目录运行：

```powershell
npm test
```

Android 调试包：

```powershell
npx cap sync android
Set-Location android
.\gradlew.bat assembleDebug
```

## 版本

- Android `versionCode 6`
- Android `versionName 1.3.2`
- npm package `1.3.2`
